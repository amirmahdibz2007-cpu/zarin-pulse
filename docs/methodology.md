# روش‌شناسی

اعداد محصول از artifactهای ETL می‌آیند. هر عدد یک Passport دارد: فرمول، SQL، دانه، اندازه نمونه، `sourceSha256`.

صفحه `/methodology` سیزده فرضیه را نشان می‌دهد. صفحه `/reconciliation` اتحادهای عددی را زنده چک می‌کند.

جزئیات دانه و تله‌ها: [`data-constitution.md`](data-constitution.md)

سیاهه اجرای تست: [`verification-log.md`](verification-log.md)
